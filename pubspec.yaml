name: baby_incubator
description: "Infant Incubator Monitoring App - Real-time vitals monitoring via ESP32 WiFi"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  http: ^1.2.2
  fl_chart: ^0.70.2
  google_fonts: ^6.2.1
  shared_preferences: ^2.3.4
  flutter_local_notifications: ^18.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
